pi_tuple = (3,1,4,1,5,9) # Tuples are lists that cannot be changed

new_tuple = list(pi_tuple) # Convert a tuple into a list
new_list = tuple(new_tuple) # Convery a list into a tuple

len(tuple) # Get the length of a tuple
min(tuple)
max(tuple)
